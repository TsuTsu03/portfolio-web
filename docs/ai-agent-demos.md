# AI agent demo provenance

The three public pages are linked from `/#agents` and `/agents`.

- **Mara** (`/agents/mara`) now displays a full-size StackWise growth-desk screenshot and embeds `/agent-demos/mara/index.html`. The public interface reuses the source project's `src/ui/styles.html` styling and portrait, with a standalone fictional workflow for briefing, leads, discovery, draft review, approval and replies. No `google.script.run` code, mailbox, credentials or private records are copied from `C:\CodingProjects\sales-marketing-agent`. The two workspace screenshots were captured from this sanitized public interface.
- **Alfred** (`/agents/alfred`) now displays the Dark Knight HUD and embeds `/agent-demos/alfred/index.html`. This public version uses the source project's static HUD markup, stylesheet and background from `C:\CodingProjects\ai-secretary-alfred\app\static`, but replaces its private `app.js` and `hud.js` with a small fixture-only `demo.js`. It has fictional briefing, file lookup, sample mail and calendar approval interactions. No private files, calendar, mail, credentials, microphone access or model provider are connected. Both HUD screenshots were captured from this public version.
- **AI Front Desk** (`/agent-demos/front-desk/index.html`) is a copy of the static production build from `C:\CodingProjects\ai-hotel-frontdesk\dist`. Its handoff is `PORTFOLIO_HANDOFF.md` in that project. The build runs entirely in the browser against fictional hotel data. The copied `index.html` adds `noindex, follow`; `/agents/front-desk` is the canonical explanatory page.

The homepage and `/agents` gallery use wide screenshots of each agent's interface. Each detail page shows two screenshots and embeds the interactive interface with a full-screen link. The hotel screenshots come from `C:\CodingProjects\ai-hotel-frontdesk\screenshots`.

When refreshing the front desk demo, run the source project's `npm run verify`, replace only `public/agent-demos/front-desk/` with the new `dist/` contents, restore the `noindex, follow` meta tag, and run this portfolio's build, SEO QA and browser flow. Browser voice still requires a separate microphone and device check; typed requests are the reliable fallback.

For Mara or Alfred visual changes, review the corresponding source project's current interface first, update only its public assets and fixture interface here, then recapture the screenshots from the public demo. Keep the original private API clients out of `public/agent-demos/`. Check an embedded workflow in a real browser at desktop and mobile widths before publishing.
